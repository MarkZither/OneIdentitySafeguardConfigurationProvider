# OneIdentitySafeguard.Mock.Server - ASP.NET Core 2.0 Server

No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)

## Run

Linux/OS X:

```
cd ../..
sh build.sh
```

Windows:

```
cd ..\..\
build.bat
```

## Run in Docker

```
docker build -t OneIdentitySafeguard.Mock.Server .
docker run -p 5000:5000 OneIdentitySafeguard.Mock.Server
```
